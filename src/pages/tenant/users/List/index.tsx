import { PageContainer } from '@/components';
import { Typography } from 'antd';

export default function UserListPage() {
  return (
    <PageContainer title="用户管理">
      <Typography.Text type="secondary">用户列表页面（待实现）</Typography.Text>
    </PageContainer>
  );
}
